// Package config loads and exposes the node agent's configuration (from
// /etc/runos/config.yaml and environment overrides), including control-plane and
// installer host resolution.
package config
